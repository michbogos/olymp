#include<stdio.h>

int main(){
    int t;
    scanf("%d", &t);
    wile(t--){
        int n;
        scanf("%d", &n);
        int dp[n+1] = {0};
        for(int i = 1; i < n+1; i++){
            int num;
            scanf("%d", &num);
            if(num > i){
                dp[];
            }
        }
    }
    return 0;
}