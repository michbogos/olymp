#include<stdio.h>

int main(){
    int a, b;
    scanf("%d.%d\n", &a, &b);
    if(a < 0) a *= -1;
    if(b < 0) b *= -1;
    int s1, s2 = 0;
    int fac = 10;
    for(int i = 0; i < 10; i++){
        
    }
    return 0;
}