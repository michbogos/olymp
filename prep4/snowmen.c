#include<stdio.h>

struct Node{
    int value;
    struct Node* parent;
};

int main(){
    struct Node *snowmen;
    snowmen->value = 0;
    snowmen->parent = NULL;
    int n, a, b;
    scanf("%d", &n);
    for(int i = 0; i < n; i++){
        scanf("%d %d", a, b);
        
    }
    return 0;
}